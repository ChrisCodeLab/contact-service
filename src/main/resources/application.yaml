server:
  port: 8080

spring:
  application:
    name: contact-service

  datasource:
    url: jdbc:postgresql://localhost:5432/contact_service_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true